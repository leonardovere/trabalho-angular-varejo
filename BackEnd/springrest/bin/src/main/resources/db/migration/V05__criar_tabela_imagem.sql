CREATE TABLE imagem(
	codigo BIGINT(20) PRIMARY KEY AUTO_INCREMENT,
	produto_imagem BIGINT,
	NOME VARCHAR(100),
	FORMATO VARCHAR(20),
	CAMINHO VARCHAR(250)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

ALTER TABLE IMAGEM ADD CONSTRAINT FK_IMAGEM_PRODUTO FOREIGN KEY(PRODUTO_IMAGEM) REFERENCES PRODUTO(CODIGO);